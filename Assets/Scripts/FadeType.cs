using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum FadeType
{
    NONE = 0,
    Linear,
    QuadraticIn,
    QuadraticOut,
    ExpoIn,
    ExpoOut
}

public enum FadeState {
    NONE = 0,
    In = 1,
    Out = 2
}
